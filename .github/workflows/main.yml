name: Deploy for preview
on:
  push:
    paths:
      - mailtest/test2.txt
       Add-Content -Path $D:a\TestApp_new\TestApp_new\mailtest\test2.txt -Value "123"
     
jobs:
  upload:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2.3.4
      
      - name: Check if File Pushed
        run: |
          echo "push Process success."

