name: Deploy for preview
on:
  push:
    paths:
      - mailtest/test2.txt
    
jobs:
  upload:
    runs-on: windows-latest
    
    steps:
      - uses: actions/checkout@v2.3.4

      - name: Modify File
        run: |
          echo "abc" > mailtest/test2.txt

      - name: Check if File Pushed
        id: Modify File
        run: |
          echo "push Process success"
