name: Deploy for preview
on:
  push:
    paths:
      - mailtest/test2.txt
    
jobs:
  upload:
    runs-on: windows-latest
    
    steps:
      - uses: actions/checkout@v2.3.4

      - name: Check if File Pushed
        run: |
          if (Test-Path "mailtest/test2.txt") {
            echo "File was pushed."
          } else {
            echo "File was not pushed."
            exit 1
          }


      - name: Perform Action
        run: |
          echo "Performing the action..." 
