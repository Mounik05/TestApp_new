name: Releases
on:
  push:
   branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
       
    steps: 
      - uses: actions/checkout@v2.3.4
      
    
      - name: setup-msbuild
        uses: microsoft/setup-msbuild@v1.1
        with:
          msbuild-architecture: x64
          
      - name: Build app for release 
        run: msbuild TestApp.sln -t:rebuild /p:Platform=x64 /p:Configuration=Release /p:LocalizedBuild=true
        
      - name: Download Logs
        uses: suisei-cn/actions-download-file@v1.0.1
        with:
          url: https://github.com/Mounik05/TestApp_new/suites/1446142----/logs?attempt=1
          headers: |
            Accept: application/vnd.github+json
            Authorization: Bearer ${{ secrets.YOUR_TOKEN }}
            X-GitHub-Api-Version: 2022-11-28
            
            
            
            
