name: Send email
on:
  push:
   branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
 send-email:
   runs-on: windows-latest
   steps:
     - name: Send Email to Single Recipient.
       uses: dawidd6/action-send-mail@v3.7.1
       with:
          server_address: smtp.gmail.com
          server_port: 465 
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          from: Kallepallimounika333@gmail.com
          To: kallepallimounika123@yahoo.com
          subject: Example email with attachment
          body: This is an example email with an attachment.
          filepath: C:\VCollab_Suite_22.3.5_Win64_221212.zip 
          attachments: attachments.zip,git.diff,./dist/static/*.js
          
        
      
          
        
          
     
