name: Deploy for preview
on:
  push:
    paths:
      - mailtest/test2.txt
jobs:
  upload:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2.3.4

      - name: Check if File Pushed
        run: |
          echo "push Process success."
          
          # Modify the file using shell commands
          echo "1123" > mailtest/test2.txt

      - name: Perform Action
        run: |
          echo "Performing the action..."
