name: Add commit message and send email
on:
  push:
   branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
 send-email:
   runs-on: windows-latest
   steps:
     - uses: actions/checkout@v2.3.4
     - name: Create commit message file
       run: |
         echo "${{ github.event.head_commit.message }}" > test.txt
     - name: Commit message file
       run: |
          git config --global user.email "mounikakallepalli333@gmail.com"
          git config --global user.name "Mounik05"
          git add test.txt
          git commit -m "new file upload"
          
     - name: Send Email to Single person
       uses: dawidd6/action-send-mail@v3.7.1
       with:
           server_address: smtp.gmail.com
           server_port: 465 
           username: ${{ secrets.MAIL_USERNAME }}
           password: ${{ secrets.MAIL_PASSWORD }}
           subject: "New commit on master branch"
           To: mounikakallepalli@vcollab.com
           from: Kallepallimounika333@gmail.com
           attachments: D:\a\TestApp_new\TestApp_new\mailtest\test.txt
           attachment: New-Object System.Net.Mail.Attachment($attachmentPath)
            attachment.Name:$attachmentName
            mailMessage.Attachments.Add($attachment)
           
        
          
         
          
        
      
          
        
          
     
